{
  "name": "claude-second-brain-kb",
  "private": true,
  "description": "Knowledge-base workspace wrapper for the ClaudeSecondBrain desktop app.",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ../ClaudeSecondBrainApp run dev",
    "app": "npm --prefix ../ClaudeSecondBrainApp run dev",
    "mcp": "node mcp-server/index.js",
    "mcp:dev": "node --watch mcp-server/index.js",
    "mcp:tunnel": "cloudflared tunnel --url http://localhost:${PORT:-3456}",
    "sync": "./sync.sh",
    "ingest": "./ingest.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "zod": "^4.2.0"
  }
}
