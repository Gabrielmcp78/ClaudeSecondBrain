# Handoff Note
**From:** Gemini
**Session date:** 2026-06-08

## What I did
- Unified the SSE and Streamable HTTP servers into a single codebase in [mcp-server/index.js](file:///Volumes/Ready500/DEVELOPMENT/ClaudeSecondBrain/mcp-server/index.js) running on port 3456. It registers both the low-level filesystem tools and the high-level semantic tools (`secondbrain.*`).
- Symlinked the active server configuration in the `gm-mcp-hub` directory to point to the new codebase, making the active launchd agent (`com.gabrielmcp.secondbrain.mcp-server`) run the unified server.
- Resolved the Tailscale CLI connection errors by opening the Tailscale GUI app (`open -a Tailscale`) and activating the public funnel on port 3456:
  ```bash
  tailscale funnel --bg 3456
  ```
- Verified that the server is successfully exposed over the internet at `https://gabrielsmini.tail26536d.ts.net/health`.
- Updated [CHATGPT_SYSTEM_PROMPT.md](file:///Volumes/Ready500/DEVELOPMENT/ClaudeSecondBrain/CHATGPT_SYSTEM_PROMPT.md) with the permanent Tailscale Funnel endpoint `https://gabrielsmini.tail26536d.ts.net/mcp` and documented all of the semantic and low-level tools available to ChatGPT.
- Appended the log of actions to [change-log.md](file:///Volumes/Ready500/DEVELOPMENT/ClaudeSecondBrain/wiki/_meta/change-log.md).

## What I left unfinished
- None of the target goals for the ChatGPT MCP connection were left incomplete.
- The `core-principles/` layer is still empty, which Claude Code flagged as the highest-priority knowledge architecture gap.

## What I want the next AI to know
- Tailscale on macOS requires the GUI helper app to be running in the menu bar, or commands will fail with `CLIError error 3`. If the tunnel drops or resolves offline, make sure to ensure the GUI application is opened (`open -a Tailscale`).
- The unified server now handles BOTH Gemini SSE connections (at `/sse` and `/messages`) and ChatGPT Streamable HTTP connections (at `/mcp`). Do not run separate instances or change the port without coordinating both.

## Hot files right now
- [mcp-server/index.js](file:///Volumes/Ready500/DEVELOPMENT/ClaudeSecondBrain/mcp-server/index.js) — the unified server script.
- [CHATGPT_SYSTEM_PROMPT.md](file:///Volumes/Ready500/DEVELOPMENT/ClaudeSecondBrain/CHATGPT_SYSTEM_PROMPT.md) — updated system prompt ready for ChatGPT.
- [change-log.md](file:///Volumes/Ready500/DEVELOPMENT/ClaudeSecondBrain/wiki/_meta/change-log.md) — session activity logged.

## Open question I'm handing off
- Should we now begin registering the `core-principles/` notes (resonance, emergence, translation) or proceed with checking the ChatGPT/Atlas connector integration live?

## Confidence level on recent wiki writes (1–10)
- 10/10. The codebase runs successfully, supports both transports natively, and was verified publicly over the Tailscale Funnel.
