# Handoff Note
**From:** Claude (Cowork session)
**Session date:** 2026-06-05
**Session duration:** ~4 hours

## What I did
- Renamed repo reference starTrekops → ClaudeSecondBrain in CLAUDE.md
- Created Gemini.md — parallel librarian schema
- Created ingest.sh — launchd-triggered ingestion via Claude Code CLI
- Built secondbrain-mcp-server (HTTP+SSE, 8 tools, port 3456)
- Set up Tailscale Funnel permanent endpoint: https://gabrielsmini.tail26536d.ts.net/sse
- Wrote bridge.js — Gemini API ↔ MCP via mcpToTool()
- Set up rclone Google Drive sync with zone-based ownership rules
- First ingestion ran automatically — 10 raw files processed, 16 wiki articles created
- Created GEMINI_SYSTEM_PROMPT.md and CHATGPT_SYSTEM_PROMPT.md
- Implemented GPT's architecture upgrade: Decision Records, Active Tensions (open-questions.md), Intellectual Lineage, Prediction Log taxonomy
- Created this handoff protocol
- Created core-principles/ directory (empty, needs population)

## What I left unfinished
- `core-principles/` directory is empty — the first-principles layer (resonance.md, entrainment.md, translation.md, meaning.md, emergence.md, identity.md, coherence.md) needs to be written. This is the most important structural work remaining.
- `wiki/decision-records/` and `wiki/intellectual-lineage/` and `wiki/prediction-log/` directories created but empty — need first articles
- GeminiChatter macOS app cleanup is deferred to a Claude Code session
- The `/call` REST endpoint on the MCP server was started but not completed
- Gemini still not fully operational (no autonomous Drive access in current interface)

## What I want the next AI to know
- GPT gave excellent architectural feedback this session — it's in the session output at `outputs/2026-06-05_session-infrastructure-buildout.md` and summarized in this note. The core insight: the system is secretly about a handful of recurring first principles (resonance, translation, meaning, emergence) that appear across all domains in different clothes. Building the core-principles/ layer is the highest-leverage next move.
- The startup-kit.sh script at repo root generates a paste-ready session context for interfaces without Drive access. Not ideal — the right fix is GeminiChatter.
- The handoff protocol (this directory) is new as of this session. The next AI should establish it as habit.
- The rclone sync runs every 10 minutes. Wiki state on Google Drive lags local by up to 10 minutes.

## Hot files right now
- `wiki/_index.md` — updated this session, reflects current 16-article state
- `wiki/_connections.md` — updated with all cross-domain links from first ingestion
- `wiki/_meta/open-questions.md` — brand new, seeded with 8 active tensions
- `handoff/HANDOFF_PROTOCOL.md` — the protocol itself, new this session
- `GEMINI_SYSTEM_PROMPT.md` — ready to paste into Google AI Studio
- `CHATGPT_SYSTEM_PROMPT.md` — GPT is using this and it's working

## Open question I'm handing off
The most pressing unresolved tension: is the core-principles/ layer a separate section of the wiki, or does it sit above the wiki entirely as a meta-layer? GPT suggested "above everything" — a hidden layer that everything connects upward to. I lean toward keeping it inside the wiki for consistency, but it may need its own navigation logic.

## Confidence level on recent wiki writes
7/10. The first ingestion was automated via Claude Code CLI — the wiki articles were written by a separate Claude Code instance without human review. They should be checked for accuracy, especially the physics content (GHRM, Formal Resonance Model) where hallucination risk is higher.
